/*
 * Copyright 2024 RAW Labs S.A.
 *
 * Use of this software is governed by the Business Source License
 * included in the file licenses/BSL.txt.
 *
 * As of the Change Date specified in that file, in accordance with
 * the Business Source License, use of this software will be governed
 * by the Apache License, Version 2.0, included in the file
 * licenses/APL.txt.
 */

package raw.compiler.rql2.api

object EntryExtensionProvider {

  private val entryExtensions: Array[EntryExtension] = Array(
    new raw.compiler.rql2.builtin.AwsV4SignedRequest,
    new raw.compiler.rql2.builtin.FromStringBinaryEntryExtension,
    new raw.compiler.rql2.builtin.BinaryReadEntry,
    new raw.compiler.rql2.builtin.BinaryBase64Entry,
    new raw.compiler.rql2.builtin.ByteFromEntry,
    new raw.compiler.rql2.builtin.EmptyCollectionEntry,
    new raw.compiler.rql2.builtin.BuildCollectionEntry,
    new raw.compiler.rql2.builtin.FilterCollectionEntry,
    new raw.compiler.rql2.builtin.AvgCollectionEntry,
    new raw.compiler.rql2.builtin.OrderByCollectionEntry,
    new raw.compiler.rql2.builtin.TransformCollectionEntry,
    new raw.compiler.rql2.builtin.DistinctCollectionEntry,
    new raw.compiler.rql2.builtin.CountCollectionEntry,
    new raw.compiler.rql2.builtin.TupleAvgCollectionEntry,
    new raw.compiler.rql2.builtin.MinCollectionEntry,
    new raw.compiler.rql2.builtin.MaxCollectionEntry,
    new raw.compiler.rql2.builtin.SumCollectionEntry,
    new raw.compiler.rql2.builtin.FirstCollectionEntry,
    new raw.compiler.rql2.builtin.FindFirstCollectionEntry,
    new raw.compiler.rql2.builtin.LastCollectionEntry,
    new raw.compiler.rql2.builtin.FindLastCollectionEntry,
    new raw.compiler.rql2.builtin.TakeCollectionEntry,
    new raw.compiler.rql2.builtin.ExplodeCollectionEntry,
    new raw.compiler.rql2.builtin.UnnestCollectionEntry,
    new raw.compiler.rql2.builtin.FromCollectionEntry,
    new raw.compiler.rql2.builtin.GroupCollectionEntry,
    new raw.compiler.rql2.builtin.JoinCollectionEntry,
    new raw.compiler.rql2.builtin.InternalJoinCollectionEntry,
    new raw.compiler.rql2.builtin.EquiJoinCollectionEntry,
    new raw.compiler.rql2.builtin.InternalEquiJoinCollectionEntry,
    new raw.compiler.rql2.builtin.UnionCollectionEntry,
    new raw.compiler.rql2.builtin.ExistsCollectionEntry,
    new raw.compiler.rql2.builtin.ContainsCollectionEntry,
    new raw.compiler.rql2.builtin.ZipCollectionEntry,
    new raw.compiler.rql2.builtin.MkStringCollectionEntry,
    new raw.compiler.rql2.builtin.CsvInferAndReadEntry,
    new raw.compiler.rql2.builtin.CsvReadEntry,
    new raw.compiler.rql2.builtin.CsvInferAndParseEntry,
    new raw.compiler.rql2.builtin.CsvParseEntry,
    new raw.compiler.rql2.builtin.DateBuildEntry,
    new raw.compiler.rql2.builtin.DateFromEpochDayEntry,
    new raw.compiler.rql2.builtin.DateFromTimestampEntry,
    new raw.compiler.rql2.builtin.DateParseEntry,
    new raw.compiler.rql2.builtin.DateNowEntry,
    new raw.compiler.rql2.builtin.DateYearEntry,
    new raw.compiler.rql2.builtin.DateMonthEntry,
    new raw.compiler.rql2.builtin.DateDayEntry,
    new raw.compiler.rql2.builtin.DateSubtractEntry,
    new raw.compiler.rql2.builtin.DateAddIntervalEntry,
    new raw.compiler.rql2.builtin.DateSubtractIntervalEntry,
    new raw.compiler.rql2.builtin.DecimalFromEntry,
    new raw.compiler.rql2.builtin.DecimalRoundEntry,
    new raw.compiler.rql2.builtin.DoubleFromEntry,
    new raw.compiler.rql2.builtin.EnvironmentSecretEntry,
    new raw.compiler.rql2.builtin.EnvironmentScopesEntry,
    new raw.compiler.rql2.builtin.EnvironmentParameterEntry,
    new raw.compiler.rql2.builtin.ErrorBuildEntry,
    new raw.compiler.rql2.builtin.ErrorBuildWithTypeEntry,
    new raw.compiler.rql2.builtin.ErrorGetEntry,
    new raw.compiler.rql2.builtin.FloatFromEntry,
    new raw.compiler.rql2.builtin.FunctionInvokeAfterEntry,
    new raw.compiler.rql2.builtin.HttpReadEntry,
    new raw.compiler.rql2.builtin.HttpUrlEncodeEntry,
    new raw.compiler.rql2.builtin.HttpUrlDecodeEntry,
    new raw.compiler.rql2.builtin.HttpPutEntry,
    new raw.compiler.rql2.builtin.HttpDeleteEntry,
    new raw.compiler.rql2.builtin.HttpGetEntry,
    new raw.compiler.rql2.builtin.HttpHeadEntry,
    new raw.compiler.rql2.builtin.HttpOptionsEntry,
    new raw.compiler.rql2.builtin.HttpPatchEntry,
    new raw.compiler.rql2.builtin.HttpPostEntry,
    new raw.compiler.rql2.builtin.BuildIntervalEntry,
    new raw.compiler.rql2.builtin.IntervalToMillisEntryExtension,
    new raw.compiler.rql2.builtin.IntervalFromMillisEntryExtension,
    new raw.compiler.rql2.builtin.IntervalParseEntryExtension,
    new raw.compiler.rql2.builtin.IntervalYearsEntry,
    new raw.compiler.rql2.builtin.IntervalMonthsEntry,
    new raw.compiler.rql2.builtin.IntervalWeeksEntry,
    new raw.compiler.rql2.builtin.IntervalDaysEntry,
    new raw.compiler.rql2.builtin.IntervalHoursEntry,
    new raw.compiler.rql2.builtin.IntervalMinutesEntry,
    new raw.compiler.rql2.builtin.IntervalSecondsEntry,
    new raw.compiler.rql2.builtin.IntervalMillisEntry,
    new raw.compiler.rql2.builtin.IntFromEntry,
    new raw.compiler.rql2.builtin.IntRangeEntry,
    new raw.compiler.rql2.builtin.InferAndReadJsonEntry,
    new raw.compiler.rql2.builtin.ReadJsonEntry,
    new raw.compiler.rql2.builtin.InferAndParseJsonEntry,
    new raw.compiler.rql2.builtin.ParseJsonEntry,
    new raw.compiler.rql2.builtin.PrintJsonEntry,
    new raw.compiler.rql2.builtin.KryoEncodeEntry,
    new raw.compiler.rql2.builtin.KryoDecodeEntry,
    new raw.compiler.rql2.builtin.EmptyListEntry,
    new raw.compiler.rql2.builtin.BuildListEntry,
    new raw.compiler.rql2.builtin.GetListEntry,
    new raw.compiler.rql2.builtin.FilterListEntry,
    new raw.compiler.rql2.builtin.TransformListEntry,
    new raw.compiler.rql2.builtin.TakeListEntry,
    new raw.compiler.rql2.builtin.SumListEntry,
    new raw.compiler.rql2.builtin.MaxListEntry,
    new raw.compiler.rql2.builtin.MinListEntry,
    new raw.compiler.rql2.builtin.FirstListEntry,
    new raw.compiler.rql2.builtin.FindFirstListEntry,
    new raw.compiler.rql2.builtin.LastListEntry,
    new raw.compiler.rql2.builtin.FindLastListEntry,
    new raw.compiler.rql2.builtin.CountListEntry,
    new raw.compiler.rql2.builtin.ExplodeListEntry,
    new raw.compiler.rql2.builtin.UnnestListEntry,
    new raw.compiler.rql2.builtin.FromListEntry,
    new raw.compiler.rql2.builtin.UnsafeFromListEntry,
    new raw.compiler.rql2.builtin.GroupListEntry,
    new raw.compiler.rql2.builtin.JoinListEntry,
    new raw.compiler.rql2.builtin.EquiJoinListEntry,
    new raw.compiler.rql2.builtin.OrderByListEntry,
    new raw.compiler.rql2.builtin.DistinctListEntry,
    new raw.compiler.rql2.builtin.UnionListEntry,
    new raw.compiler.rql2.builtin.AvgListEntry,
    new raw.compiler.rql2.builtin.ExistsListEntry,
    new raw.compiler.rql2.builtin.ContainsListEntry,
    new raw.compiler.rql2.builtin.ZipListEntry,
    new raw.compiler.rql2.builtin.MkStringListEntry,
    new raw.compiler.rql2.builtin.LocationBuildEntry,
    new raw.compiler.rql2.builtin.LocationDescribeEntry,
    new raw.compiler.rql2.builtin.LocationLsEntry,
    new raw.compiler.rql2.builtin.LocationLlEntry,
    new raw.compiler.rql2.builtin.LongFromEntry,
    new raw.compiler.rql2.builtin.LongRangeEntry,
    new raw.compiler.rql2.builtin.MathPiEntry,
    new raw.compiler.rql2.builtin.MathRandomEntry,
    new raw.compiler.rql2.builtin.MathPowerEntry,
    new raw.compiler.rql2.builtin.MathAtn2Entry,
    new raw.compiler.rql2.builtin.MathAbsEntry,
    new raw.compiler.rql2.builtin.MathAcosEntry,
    new raw.compiler.rql2.builtin.MathAsinEntry,
    new raw.compiler.rql2.builtin.MathAtanEntry,
    new raw.compiler.rql2.builtin.MathCeilingEntry,
    new raw.compiler.rql2.builtin.MathCosEntry,
    new raw.compiler.rql2.builtin.MathCotEntry,
    new raw.compiler.rql2.builtin.MathDegreesEntry,
    new raw.compiler.rql2.builtin.MathExpEntry,
    new raw.compiler.rql2.builtin.MathLogEntry,
    new raw.compiler.rql2.builtin.MathLog10Entry,
    new raw.compiler.rql2.builtin.MathRadiansEntry,
    new raw.compiler.rql2.builtin.MathSignEntry,
    new raw.compiler.rql2.builtin.MathSinEntry,
    new raw.compiler.rql2.builtin.MathSqrtEntry,
    new raw.compiler.rql2.builtin.MathTanEntry,
    new raw.compiler.rql2.builtin.MathSquareEntry,
    new raw.compiler.rql2.builtin.MathFloorEntry,
    new raw.compiler.rql2.builtin.MySQLInferAndReadEntry,
    new raw.compiler.rql2.builtin.MySQLReadEntry,
    new raw.compiler.rql2.builtin.MySQLInferAndQueryEntry,
    new raw.compiler.rql2.builtin.MySQLQueryEntry,
    new raw.compiler.rql2.builtin.NullableEmptyEntry,
    new raw.compiler.rql2.builtin.NullableBuildEntry,
    new raw.compiler.rql2.builtin.NullableIsNullEntry,
    new raw.compiler.rql2.builtin.NullableUnsafeGetEntry,
    new raw.compiler.rql2.builtin.NullableTransformEntry,
    new raw.compiler.rql2.builtin.FlatMapNullableTryableEntry,
    new raw.compiler.rql2.builtin.OracleInferAndReadEntry,
    new raw.compiler.rql2.builtin.OracleReadEntry,
    new raw.compiler.rql2.builtin.OracleInferAndQueryEntry,
    new raw.compiler.rql2.builtin.OracleQueryEntry,
    new raw.compiler.rql2.builtin.PostgreSQLInferAndReadEntry,
    new raw.compiler.rql2.builtin.PostgreSQLReadEntry,
    new raw.compiler.rql2.builtin.PostgreSQLInferAndQueryEntry,
    new raw.compiler.rql2.builtin.PostgreSQLQueryEntry,
    new raw.compiler.rql2.builtin.RecordBuildEntry,
    new raw.compiler.rql2.builtin.RecordConcatEntry,
    new raw.compiler.rql2.builtin.RecordFieldsEntry,
    new raw.compiler.rql2.builtin.RecordAddFieldEntry,
    new raw.compiler.rql2.builtin.RecordRemoveFieldEntry,
    new raw.compiler.rql2.builtin.RecordGetFieldByIndexEntry,
    new raw.compiler.rql2.builtin.RegexReplaceEntry,
    new raw.compiler.rql2.builtin.RegexMatchesEntry,
    new raw.compiler.rql2.builtin.RegexFirstMatchInEntry,
    new raw.compiler.rql2.builtin.RegexGroupsEntry,
    new raw.compiler.rql2.builtin.S3BuildEntry,
    new raw.compiler.rql2.builtin.ShortFromEntry,
    new raw.compiler.rql2.builtin.SnowflakeInferAndReadEntry,
    new raw.compiler.rql2.builtin.SnowflakeReadEntry,
    new raw.compiler.rql2.builtin.SnowflakeInferAndQueryEntry,
    new raw.compiler.rql2.builtin.SnowflakeQueryEntry,
    new raw.compiler.rql2.builtin.SQLServerInferAndReadEntry,
    new raw.compiler.rql2.builtin.SQLServerReadEntry,
    new raw.compiler.rql2.builtin.SQLServerInferAndQueryEntry,
    new raw.compiler.rql2.builtin.SQLServerQueryEntry,
    new raw.compiler.rql2.builtin.StringFromEntry,
    new raw.compiler.rql2.builtin.StringReadEntry,
    new raw.compiler.rql2.builtin.StringContainsEntry,
    new raw.compiler.rql2.builtin.StringTrimEntry,
    new raw.compiler.rql2.builtin.StringLTrimEntry,
    new raw.compiler.rql2.builtin.StringRTrimEntry,
    new raw.compiler.rql2.builtin.StringReplaceEntry,
    new raw.compiler.rql2.builtin.StringReverseEntry,
    new raw.compiler.rql2.builtin.StringReplicateEntry,
    new raw.compiler.rql2.builtin.StringUpperEntry,
    new raw.compiler.rql2.builtin.StringLowerEntry,
    new raw.compiler.rql2.builtin.StringSplitEntry,
    new raw.compiler.rql2.builtin.StringLengthEntry,
    new raw.compiler.rql2.builtin.StringSubStringEntry,
    new raw.compiler.rql2.builtin.StringCountSubStringEntry,
    new raw.compiler.rql2.builtin.StringStartsWithEntry,
    new raw.compiler.rql2.builtin.StringEmptyEntry,
    new raw.compiler.rql2.builtin.Base64EntryExtension,
    new raw.compiler.rql2.builtin.StringEncodeEntry,
    new raw.compiler.rql2.builtin.StringDecodeEntry,
    new raw.compiler.rql2.builtin.StringLevenshteinDistanceEntry,
    new raw.compiler.rql2.builtin.StringReadLinesEntry,
    new raw.compiler.rql2.builtin.StringCapitalizeEntry,
    new raw.compiler.rql2.builtin.SuccessBuildEntry,
    new raw.compiler.rql2.builtin.MandatoryExpArgsEntry,
    new raw.compiler.rql2.builtin.MandatoryValueArgsEntry,
    new raw.compiler.rql2.builtin.OptionalExpArgsTestEntry,
    new raw.compiler.rql2.builtin.OptionalValueArgsTestEntry,
    new raw.compiler.rql2.builtin.OptionalValueArgSugar,
    new raw.compiler.rql2.builtin.VarValueArgSugarTestEntry,
    new raw.compiler.rql2.builtin.VarExpArgsTestEntry,
    new raw.compiler.rql2.builtin.VarValueArgsTestEntry,
    new raw.compiler.rql2.builtin.VarNullableStringValueTestEntry,
    new raw.compiler.rql2.builtin.VarNullableStringExpTestEntry,
    new raw.compiler.rql2.builtin.StrictArgsTestEntry,
    new raw.compiler.rql2.builtin.StrictArgsColPassThroughTestEntry,
    new raw.compiler.rql2.builtin.StrictArgsColConsumeTestEntry,
    new raw.compiler.rql2.builtin.ByteValueArgTestEntry,
    new raw.compiler.rql2.builtin.ShortValueArgTestEntry,
    new raw.compiler.rql2.builtin.IntValueArgTestEntry,
    new raw.compiler.rql2.builtin.LongValueArgTestEntry,
    new raw.compiler.rql2.builtin.FloatValueArgTestEntry,
    new raw.compiler.rql2.builtin.DoubleValueArgTestEntry,
    new raw.compiler.rql2.builtin.StringValueArgTestEntry,
    new raw.compiler.rql2.builtin.BoolValueArgTestEntry,
    new raw.compiler.rql2.builtin.DateValueArgTestEntry,
    new raw.compiler.rql2.builtin.TimeValueArgTestEntry,
    new raw.compiler.rql2.builtin.TimestampValueArgTestEntry,
    new raw.compiler.rql2.builtin.IntervalValueArgTestEntry,
    new raw.compiler.rql2.builtin.RecordValueArgTestEntry,
    new raw.compiler.rql2.builtin.OrValueArgTestEntry,
    new raw.compiler.rql2.builtin.ListValueArgTestEntry,
    new raw.compiler.rql2.builtin.TimeBuildEntry,
    new raw.compiler.rql2.builtin.TimeParseEntry,
    new raw.compiler.rql2.builtin.TimeNowEntry,
    new raw.compiler.rql2.builtin.TimeHourEntry,
    new raw.compiler.rql2.builtin.TimeMinuteEntry,
    new raw.compiler.rql2.builtin.TimeSecondEntry,
    new raw.compiler.rql2.builtin.TimeMillisEntry,
    new raw.compiler.rql2.builtin.TimeSubtractEntry,
    new raw.compiler.rql2.builtin.TimeAddIntervalEntry,
    new raw.compiler.rql2.builtin.TimeSubtractIntervalEntry,
    new raw.compiler.rql2.builtin.TimestampBuildEntry,
    new raw.compiler.rql2.builtin.TimestampFromDateEntry,
    new raw.compiler.rql2.builtin.TimestampParseEntry,
    new raw.compiler.rql2.builtin.TimestampNowEntry,
    new raw.compiler.rql2.builtin.TimestampRangeEntry,
    new raw.compiler.rql2.builtin.TimestampYearEntry,
    new raw.compiler.rql2.builtin.TimestampMonthEntry,
    new raw.compiler.rql2.builtin.TimestampDayEntry,
    new raw.compiler.rql2.builtin.TimestampHourEntry,
    new raw.compiler.rql2.builtin.TimestampMinuteEntry,
    new raw.compiler.rql2.builtin.TimestampSecondEntry,
    new raw.compiler.rql2.builtin.TimestampMillisEntry,
    new raw.compiler.rql2.builtin.TimestampFromUnixTimestampEntry,
    new raw.compiler.rql2.builtin.TimestampToUnixTimestampEntry,
    new raw.compiler.rql2.builtin.TimestampTimeBucketEntry,
    new raw.compiler.rql2.builtin.TimestampSubtractEntry,
    new raw.compiler.rql2.builtin.TimestampAddIntervalEntry,
    new raw.compiler.rql2.builtin.TimestampSubtractIntervalEntry,
    new raw.compiler.rql2.builtin.TryTransformEntry,
    new raw.compiler.rql2.builtin.TryIsErrorEntry,
    new raw.compiler.rql2.builtin.TryIsSuccessEntry,
    new raw.compiler.rql2.builtin.TryFlatMapEntry,
    new raw.compiler.rql2.builtin.TryUnsafeGetEntry,
    new raw.compiler.rql2.builtin.TypeCastEntry,
    new raw.compiler.rql2.builtin.TypeProtectCastEntry,
    new raw.compiler.rql2.builtin.TypeEmptyEntry,
    new raw.compiler.rql2.builtin.TypeMatchEntry,
    new raw.compiler.rql2.builtin.InferAndReadXmlEntry,
    new raw.compiler.rql2.builtin.ReadXmlEntry,
    new raw.compiler.rql2.builtin.ParseXmlEntry
  )

  def getEntries(name: String): Array[EntryExtension] = {
    entryExtensions.filter(_.packageName == name)
  }

}
